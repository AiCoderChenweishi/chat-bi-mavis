# 数据分析 Agent 环境变量模板
# 部署时: cp .env.example .env && vim .env 填入真实 key

# LLM 至少配一个
DEEPSEEK_API_KEY=sk-your-deepseek-key
# MINIMAX_API_KEY=your-minimax-key

# 不推荐用这种方式配 base URL(llm_client.py 硬编码)
# DEEPSEEK_BASE_URL=https://api.deepseek.com/v1
